#include "header.h"
#include <stdio.h>
#include <stdlib.h>

// Use the command to complie this program.
// gcc -o main main.c function.c

int main(int argc, char const *argv[])
{
    toString();
    // Freeze the screen using getChar() or system("pause");
    // getchar();
    system("pause");
    return 0;
}